<% if current_user.following?(tweet.user) %>
  <%= link_to "Unfollow", user_relationships_path(tweet.user.id), method: :delete %>
<% else %>
  <%= link_to "Follow", user_relationships_path(tweet.user.id), method: :post %>
<% end %>
 </th>
  <%= notifications = @notifications.where.not(visitor_id: current_user.id) %>
  <% if notifications.exists? %>
   <%= render notifications %>
  <% else %>
   <<p> 通知はありません </p>>
   </table>
  <% end %>